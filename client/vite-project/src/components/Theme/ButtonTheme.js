import { createTheme } from "@mui/material";


export const Buttontheme = createTheme({
    palette: {
      primary: {
        main: '#3c3c3c',
      },
      secondary: {
        main: '#dedede',
      },
    },
  });